setup:
  addons:
    - plan: heroku-postgresql
      as: postgres
build:
  docker:
    client: ./client/Dockerfile.dev
    api: ./api/Dockerfile
    nginx: ./nginx/Dockerfile.dev
  config:
    RAILS_ENV: development
    FOO: bar
run:
  client: npm run start
  api: npm run start
